# Handoff

Project: `/home/commander/projects/open-asset-lab`, branch `main`. Open Halo: `/home/commander/projects/halo-trial-android`, branch `fp-animated-guns`. Never commit real map/package/image artifacts. Local real test data: `/home/commander/assetlab-private/maps/`. Staging: `~/.local/share/open-asset-lab/staged/`.

Run `python -m unittest discover -s tests -v`. Build Open Halo with `cmake -S . -B build-host -G Ninja && cmake --build build-host`; run `scripts/verify.sh` with `HTA_MAP=/home/commander/halo-trial-data/extract/maps/bloodgulch.map` and `VK_ICD_FILENAMES=$PWD/scratch/lvp/usr/share/vulkan/icd.d/lvp_icd.json`. Host proof: `build-host/open-halo-map-test /path/to/package.oalmap /private/output-prefix`.

Service: `.venv/bin/python -m assetlab serve --tailscale --source-dir /home/commander/assetlab-private/maps --vpk /home/commander/assetlab-private/tf2-server/tf/tf2_misc_dir.vpk --vpk /home/commander/assetlab-private/tf2-server/tf/tf2_textures_dir.vpk`. Access credentials: `.venv/bin/python -m assetlab access`. Port 8762 is separate from Open Halo sideload port 8731. The phone was offline at initial verification; remote browser behavior still needs an actual device test. The anonymous TF2 dedicated-server install has material definitions but lacks numbered texture data archives, so the latest staged preview uses honest material-color placeholders for 66 of 71 surfaces. A complete local TF2 client install or user-supplied materials are needed for actual textures. Android has no external-map picker or runtime mode yet. See [PROGRESS.md](PROGRESS.md) for verified results and [OPEN_HALO_INTEGRATION.md](OPEN_HALO_INTEGRATION.md) before editing engine code.
